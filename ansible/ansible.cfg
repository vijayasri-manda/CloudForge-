[defaults]
inventory = inventory/hosts.yml
remote_user = opc
host_key_checking = False
timeout = 30
stdout_callback = yaml
roles_path = roles

[privilege_escalation]
become = True
become_method = sudo
become_user = root
